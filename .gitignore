# Godot 4+ specific ignores
.godot/
.godot/**
*.translation
export.cfg
# NOTE: export_presets.cfg is intentionally tracked (not ignored) — the Docker
# build's `godot --export-release "Web"` step needs it present in the checked-
# out source, same as godot-rpg's repo does.
build/

# Mono-specific ignores
.mono/
data_*/
mono_crash.*.json

# System/tool-specific ignores
.DS_Store
